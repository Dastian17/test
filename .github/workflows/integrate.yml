name: javascript-CI

on:
  push:
    branches: [main]  # Ejecutar en cada push a la rama 'main'
  pull_request:
    branches: [main, master]  # También ejecutar en PRs hacia 'main' y 'master'

permissions: write-all

jobs:
  ci_to_main:
    runs-on: ubuntu-latest
    steps:
      # Checkout the repository
      - uses: actions/checkout@v4
      
      # Set up Node.js version 20
      - name: Set up node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
      
      # Install dependencies using npm ci
      - name: Install dependencies
        run: npm ci
      
      # Run Unit Tests
      - name: Run Unit Tests
        run: npm run test-once
      
      # Generate Jest coverage report
      - name: Jest coverage report
        uses: ArtiomTr/jest-coverage-report-action@v2.2.6
        id: coverage
        with:
          output: report-markdown, comment
      
      # Add Jest coverage report as a sticky comment on commit or pull request
      - uses: marocchino/sticky-pull-request-comment@v2
        if: github.event_name == 'push'
        with:
          message: ${{ steps.coverage.outputs.report }}
      
      #####
      # Run E2E Tests (optional, commented for now)
      # - name: Run E2E Tests
      #   uses: cypress-io/github-action@v4
      #   with:
      #     start: npm start
      #     wait-on: "http://localhost:1234"
      
      # Build for deployment (optional, commented for now)
      # - name: Build for deploy
      #   run: npm run build
      
      # Deploy to GitHub Pages (optional, commented for now)
      # - name: Deploy to GitHub Pages
      #   if: success()
      #   uses: crazy-max/ghaction-github-pages@v2
      #   with:
      #     target_branch: gh-pages
      #     build_dir: dist
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
